<?xml version="1.0" encoding="utf-8" ?>
<root>
    <client id="client1">
        <product>

            <byId>
                select  products_id as id, products_name as name, products_price as price,
                categories_id as categoryId, categories_name as categoryName, manufacturers_name as manufacturer,
                products_image as imageUrl, products_url as url, 0 as categoryParentId,
                products_short_description as shortDescription, products_description as description,
                products_status = 'yes' OR products_status = '1' OR products_status = 'true' as published
                from products
                left join products_description using(products_id)
                left join products_to_categories using(products_id)
                left join categories_description using(categories_id)
                left join manufacturers using(manufacturers_id)
                where products_id = ?
            </byId>

            <listOfId>
                select  products_id as id, products_name as name, products_price as price,
                        categories_id as categoryId, categories_name as categoryName, manufacturers_name as manufacturer,
                        products_image as imageUrl, products_url as url, 0 as categoryParentId,
                        products_short_description as shortDescription, products_description as description,
                        products_status = 'yes' OR products_status = '1' OR products_status = 'true' as published
                from products
                    left join products_description using(products_id)
                    left join products_to_categories using(products_id)
                    left join categories_description using(categories_id)
                    left join manufacturers using(manufacturers_id)
                where products_id IN (?)
            </listOfId>

            <byCategoryId>
                select  products_id as id, products_name as name, products_price as price,
                categories_id as categoryId, categories_name as categoryName, manufacturers_name as manufacturer,
                products_image as imageUrl, products_url as url,0 as categoryParentId,
                products_short_description as shortDescription, products_description as description,
                products_status = 'yes' OR products_status = '1' OR products_status = 'true' as published
                from products
                left join products_description using(products_id)
                left join products_to_categories using(products_id)
                left join categories_description using(categories_id)
                left join manufacturers using(manufacturers_id)
                where categories_id = ?
            </byCategoryId>

            <byProductSku>
                SELECT name, price, category From Product WHERE productSku = ?
            </byProductSku>

        </product>

        <categories>
            <getCategories>
                SELECT categories_id as categoryId, categories_name as categoryName, 0 as parentId FROM categories_description
            </getCategories>

            <getParentCategories>
                SELECT categories_id as categoryId, categories_name as categoryName, 0 as parentId FROM categories_description
                WHERE categories_description.categories_id IN (?)
            </getParentCategories>
        </categories>


    </client>

    <client id="client2">
        <product>

                    <byId>
                        select  product_id as id, oc_product_description.name as `name`, oc_product.price as price,
                                        category_id as categoryId, oc_category_description.name as categoryName,
                        				oc_manufacturer.name as manufacturer,
                                        oc_product.image as imageUrl, oc_category.parent_id as categoryParentId,
                                        oc_product_description.description as description,
                                        oc_product.status = 'yes' OR oc_product.status = '1' OR oc_product.status = 'true' as published
                        from oc_product
                        left join oc_product_description using(product_id)
                        left join oc_product_to_category using(product_id)
                        left join oc_category using(category_id)
                        left join oc_category_description using(category_id)
                        left join oc_manufacturer using(manufacturer_id)
                        where product_id = ?
                    </byId>

                    <listOfId>
                        select  product_id as id, oc_product_description.name as `name`, oc_product.price as price,
                                        category_id as categoryId, oc_category_description.name as categoryName,
                                        oc_manufacturer.name as manufacturer,
                                        oc_product.image as imageUrl, oc_category.parent_id as categoryParentId,
                                        oc_product_description.description as description,
                                        oc_product.status = 'yes' OR oc_product.status = '1' OR oc_product.status = 'true' as published
                        from oc_product
                        left join oc_product_description using(product_id)
                        left join oc_product_to_category using(product_id)
                        left join oc_category using(category_id)
                        left join oc_category_description using(category_id)
                        left join oc_manufacturer using(manufacturer_id)
                        where product_id IN (?)
                    </listOfId>

                    <byCategoryId>
                        select  product_id as id, oc_product_description.name as `name`, oc_product.price as price,
                                        category_id as categoryId, oc_category_description.name as categoryName,
                                        oc_manufacturer.name as manufacturer,
                                        oc_product.image as imageUrl, oc_category.parent_id as categoryParentId,
                                        oc_product_description.description as description,
                                        oc_product.status = 'yes' OR oc_product.status = '1' OR oc_product.status = 'true' as published
                        from oc_product
                        left join oc_product_description using(product_id)
                        left join oc_product_to_category using(product_id)
                        left join oc_category using(category_id)
                        left join oc_category_description using(category_id)
                        left join oc_manufacturer using(manufacturer_id)
                        where category_id = ?
                    </byCategoryId>

                    <byProductSku>

                    </byProductSku>

        </product>

        <categories>
            <getCategories>
                SELECT category_id as categoryId, oc_category_description.name as categoryName,
                oc_category.parent_id as categoryParentId
                from oc_category
                left join oc_category_description using(category_id)
            </getCategories>

            <getParentCategories>
                SELECT parent_category.category_id as categoryId, oc_category_description.`name` as categoryName,
                parent_category.parent_id as categoryParentId
                FROM oc_category as category, oc_category as parent_category
                JOIN oc_category_description ON oc_category_description.category_id = parent_category.category_id
                WHERE category.parent_id = parent_category.category_id
                AND parent_category.category_id  parent_category.parent_id
                AND category.category_id IN (?)
            </getParentCategories>
        </categories>
    </client>

</root>